databases:
  - name: trilcenterprises
    databaseName: admins_d44o
    user: fonti

services:
  - type: web
    name: trilcenterprises
    runtime: python
    buildCommand: "build.bat"
    startCommand: "waitress-serve corepro.wsgi:application"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: trilcenterprises
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4